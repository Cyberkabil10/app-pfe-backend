pipeline {
    agent any
    stages {
        stage('Build artifact') {
        steps {
            sh " cd covibed_backEnd\"
            sh 'mvn clean install -DskipTests' }
        }
        }
    }
